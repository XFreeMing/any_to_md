# 书籍转换实现 TODO：人机协作与功能节点

更新日期：2026-09-16。

当前状态：M01 已实现，待用户用真实 EPUB 验证。尚未交付的后续命令、脚本和目录仍是约定内容，不代表现在已经存在或可以执行。

设计依据：

- [总体转换架构](specs/0002-conversion-architecture.md)：格式适配、统一文档模型、PDF 与 OCR 边界。
- [书籍结构与双版本导航](specs/0003-book-structure-and-navigation.md)：章节树、身份、命名、索引与目标源码目录。
- [SQLite 持久化](specs/0004-sqlite-persistence.md)：书籍版本、身份、任务和产物状态。

本计划定义实际开发顺序和协作方式。早期设计中的单测、集成验证、效果评估等设想，在本轮开发中统一按这里的分工执行；架构约束继续有效。

## 1. 协作规则

**Codex 负责实现和修复，用户负责执行、观察、验收与反馈。每次交付一个能实际使用的功能。**

- Codex 阅读相关代码、实现当前节点、做静态代码检查，交付运行/启动脚本、准确命令、输出位置及验证步骤。
- 不安排 Codex 编写或运行单元测试，不新增自动化测试工程、集成测试套件、E2E 测试或基准测试来代替用户验证。已有测试文件不因此删除。
- 功能运行、服务启动、真实文件转换和效果确认由用户执行。Codex 可以检查代码、配置、引用、语法和依赖声明，但不得据此声称功能已验证通过。
- 产品本身仍保留必要的输入校验、模型约束、资源/链接检查、错误报告和任务状态判断。这些是运行时功能，不是测试用例；不能因不写单测而移除。
- 每个节点包含实现、脚本、操作说明、反馈修复全过程。仅创建目录、接口、数据库表或安装依赖，不算一个完成节点。
- 用户没有反馈不等于验收通过。当前节点交付后停在“待用户验证”；反馈问题后进入“修复中”，直到用户明确确认该节点可用。
- 阻碍当前功能使用的问题在当前节点解决；不影响节点目标的新增需求记入后续 TODO，并说明边界。
- 默认当前节点验收后再进入下一节点。用户指定改换优先级时更新本计划，不把已知未解决问题隐藏到“已完成”。

### 节点状态

`待实现 → 实现中 → 待用户验证 → 已验收`

有问题时：`待用户验证 → 修复中 → 待用户验证`。

本文的节点总复选框仅在用户确认后勾选。实现子项可以先勾选，但节点必须仍标注“待用户验证”。本计划是唯一执行进度记录，不另维护互相冲突的 TODO 列表。

## 2. 每次交付的固定内容

1. **本次可用功能**：输入什么、可以完成什么、当前限制是什么。
2. **实现范围**：本次涉及的代码、目录或数据变动，以及对现有入口的影响。
3. **用户执行命令**：提供可直接复制的环境准备、执行或启动命令；脚本本身调用正式应用用例。
4. **产物位置**：给出实际文件/目录、书籍 ID、导出 ID 和报告位置；Web 节点给出真实访问地址。
5. **人工验证步骤**：通常 3–6 步，说明打开哪个文件、点哪个链接、观察什么；复杂节点分成多轮验证。
6. **已知限制与反馈入口**：记录需要用户重点观察的情况，并附简短反馈模板。

交付时明确区分“已实现”“已做静态检查”和“等待用户验证”。不写“测试通过”，除非用户实际反馈了对应结果。

## 3. 脚本与本地运行约定

目标位置为 `scripts/run/`，采用 Python 薄包装脚本，统一通过 `uv run python ...` 执行。Web 启动也提供脚本，复用正式应用入口。

- 以仓库根目录为工作目录；路径含中文或空格时示例统一加引号。
- `--help` 说明输入、输出、支持范围和示例，缺少参数时给出可操作信息。
- 脚本只处理参数、调用用例并显示结果，不复制 Reader、Writer、SQL 或章节解析逻辑。
- 使用统一 `--data-dir`，初期默认 `./var`；指定输入书籍不会移动、删除或覆盖原文件。
- 新导出写入独立目录，终端明确打印完整路径；重复执行保留已有版本，不靠删除整个输出目录重跑。
- 有明确的成功、部分完成、失败提示及退出码，不能有缺页或转换异常仍显示完整成功。
- 报告写入产物目录，包含 job/export ID、参数摘要、警告、实际异常位置；用户无需手动查数据库才能反馈。
- 在相关节点提供取消、重跑或恢复命令；脚本不是自动断言结果的“验收测试脚本”。

M01 拟交付的入口示例，届时以实际实现更新：

```bash
uv sync
uv run python scripts/run/convert_book.py --input "/路径/一本书.epub" --data-dir "./var" --view both
```

预期输出组织：

```text
var/
  catalog.sqlite
  sources/
  revisions/
  exports/<export_id>/
    book.md
    TOC.md
    toc.json
    locations.json
    manifest.json
    report.json
    chapters/
    assets/
```

脚本随节点建立，不在计划阶段生成空壳。功能成熟后，正式 CLI 可以复用这些参数约定；用户已使用的脚本入口尽量保持兼容。

## 4. 功能节点总览

下表中的顺序是默认实施顺序。M13 完成书籍批处理体验后，再推进可选节点；用户可基于实际反馈调整优先级。

| 状态 | 节点 | 用户获得的可用功能 | 主要前置 |
|---|---|---|---|
| [ ] | M01 | 一本 EPUB 一次得到整体/分章 MD 和可跳转目录（待用户验证） | 无 |
| [ ] | M02 | 复杂目录、中文命名、重复标题与切分粒度可控 | M01 |
| [ ] | M03 | 插图、表格、脚注和跨章引用在两份 MD 中可阅读 | M02 |
| [ ] | M04 | 查书、查历史、重新导出和保留稳定身份 | M03 |
| [ ] | M05 | 修正错误章节，重新导出两种 MD | M04 |
| [ ] | M06 | 在 Web 上传、转换、浏览目录和下载书籍包 | M05 |
| [ ] | M07 | DOCX、HTML、MD 逐个接入同一书籍转换流程 | M06；三个子节点分别验收 |
| [ ] | M08 | 文字 PDF 转双版本 MD，并按来源页定位 | M07 |
| [ ] | M09 | 复杂 PDF 的图片、表格和跨页内容可追溯阅读 | M08 |
| [ ] | M10 | 扫描/混合 PDF 经已配置 OCR 服务转 MD | M09、可运行的 OCR 服务 |
| [ ] | M11 | 本地小模型完成书籍 OCR | M10、用户设备与模型条件明确 |
| [ ] | M12 | 从已有书籍结构导出 HTML 和排版 PDF | M09；不依赖本地 OCR |
| [ ] | M13 | 批量转换书库、查看进度并恢复中断任务 | M12 |
| [ ] | M14 | 可选：AI 分块附带完整章节与双视图位置 | M04；按需求提前 |
| [ ] | M15 | 可选：网页导入同一书籍/文档流程 | M07-HTML |
| [ ] | M16 | 可选：导出 DOCX | M12 |
| [ ] | M17 | 可选：导出 EPUB | M12 |

模型或服务条件未就绪时，不标记对应 OCR 节点完成；可以按用户安排先做 M12 等不依赖它的功能。新增输入的正式支持条件始终包含 MD 输出。

## 5. 节点详细 TODO

### M01：一本 EPUB 的双版本 Markdown 闭环

状态：待用户验证。

- [x] 在 `src/any_to_md/` 建立当前用到的领域、应用、EPUB 读取与 MD 导出模块，保留现有入口。
- [x] 读取有效 EPUB 目录的完整父子层级和正文边界；建立稳定节点 ID、spine 阅读顺序和来源定位。
- [x] 以同一导出计划生成整体版、分章版、总目录和两处位置映射；章内小节不独立文件也可定位。
- [x] 按“层级序号 + 中文标题 + 短 ID”命名，部/卷索引递归排列；两种视图共用资源引用。
- [x] 接入最小 SQLite 持久化：来源、书籍/版本、不可变修订、节点身份、导出及位置记录；重复运行不覆盖旧结果。
- [x] 提供 `convert_book.py` 和输出报告；缺失目录或不能可靠解析的内容给出明确限制。

用户执行：上述 EPUB 转换命令。先用一本目录完整的书；基础图片复制沿用可复用能力，复杂结构待后续节点扩展。

用户验证：打开 TOC → 分别跳整体版与分章版 → 定位同一个小节 → 查看前后正文与基础图片 → 再执行一次确认旧产物仍在。

完成标准：这本书的两份 MD 可阅读、目录层级可见、章节与小节两处可定位，用户确认主流程可用。

交付日期：2026-09-16  
状态：待用户验证  
本轮功能/修复：新增书籍领域模型、递归 EPUB 目录、spine 阅读顺序、统一导出计划、双版本 MD、TOC/位置索引、资源目录、不可变 SQLite 修订及发布回滚；根据验证反馈，将未列入目录的 `*_split_NNN` 物理分片并回前一个真实章节，清理 EPUB 拆分章名、逐字粗体和伪标题，将正文标题保存为相对章节层级，将带行号的等宽字体代码清单转换为围栏代码块，并将整段加粗的编号子主题提升为下一层标题。  
实际执行命令：`uv run python scripts/run/convert_book.py --input "/路径/一本书.epub" --data-dir "./var" --view both`  
输出位置：命令成功后终端打印 `var/exports/<export_id>/` 和 `report.json` 的绝对路径。  
用户反馈摘要：已依次反馈并修复物理分片伪章节、章标题重复、逐字粗体、整体版小节层级未下沉、代码清单未形成 Markdown 代码块，以及编号加粗子主题碎片；v7 会合并相邻强调并把短编号主题提升为语义标题，等待同一本 EPUB 复验。  
未解决项：M01 只承诺基础图片和简单表格；复杂章界、表格、脚注及原书跨章链接分别留在 M02/M03。  
验收结论与日期：待用户确认。

### M02：复杂章节与可控文件命名

状态：待实现。

- [ ] 支持一文件多章、一章跨文件、父节点引言/尾段、无正文分组、前言和附录。
- [ ] 对缺失锚点、目录与正文冲突、无目录输入，提供回退或待修订信息，不复制整篇正文补洞。
- [ ] 完成中文、重名、非法字符、超长标题和 ID 冲突的命名处理。
- [ ] 支持按章或指定层级拆分，以及可读命名/稳定 ID 命名选项；整体版与分章版索引一起变化。
- [ ] 扩展 `convert_book.py` 的选项与报告，保留 M01 的简单命令。

用户验证：选择存在复杂目录的书 → 对照原书检查父子层级和章界 → 查看中文/重名文件 → 切换拆分选项 → 检查同一小节仍可从 TOC 定位。

完成标准：用户选定的复杂结构可正确表达，回退行为和未解决章节在报告中明确可见。

### M03：带资源与书内引用的完整阅读包

状态：待实现。

- [ ] 统一图片保存、去重和最终相对路径，整体版、分章版和 ZIP 移动后均能引用。
- [ ] 输出简单表格；复杂表格保留 HTML/结构化数据或原图，并说明展示损失。
- [ ] 完成跨章链接、重复源锚点、脚注及图表引用的两种视图重写。
- [ ] 支持整体版、分章版、双版本整包下载，导航不指向未交付文件。
- [ ] 在 `convert_book.py` 增加打包选项，提供明确的 ZIP 输出地址。

用户验证：转换一本有图表和脚注的书 → 点击跨章引用与回链 → 将 ZIP 解压到另一目录 → 再打开两种 MD 检查图片和链接。

完成标准：用户确认选定书籍可以作为完整离线阅读包使用，已知格式损失有对应原始内容或报告。

### M04：书籍历史、稳定身份与再次导出

状态：待实现。

- [ ] 补齐 SQLite 修订、产物位置和历史导出查询，长期书籍与短期任务生命周期分开。
- [ ] 支持列出已导入书籍、查看导出记录、定位产物，以及从已保存 IR 再次导出。
- [ ] 改变展示/拆分设置不重新读取原书或触发 OCR；节点身份与导出路径分开保存。
- [ ] 提供 `library.py` 的 list/show/history 和 `export_book.py` 的按修订导出入口。

用户验证：导入一本书 → 关闭进程后重新查询 → 找到旧导出 → 更换输出设置再导出 → 对照两次记录中的节点 ID 和文件位置。

完成标准：重启不丢书籍与历史；旧导出可访问，重复导出身份可追溯。

### M05：人工修正章节识别结果

状态：待实现。

- [ ] 允许导出结构修订模板，修改章名、父子关系、顺序及已有内容块处的边界。
- [ ] 应用前展示变更摘要；校验无环、来源范围和正文覆盖；保存新修订，不修改旧版。
- [ ] 重新生成双 MD、目录和路径映射；合并/拆分时记录节点对应关系。
- [ ] 提供 `edit_structure.py` 的导出模板、预览和应用入口，接受 JSON 修订文件。

用户验证：选一处错误章名或层级 → 编辑模板并预览 → 应用 → 重新导出 → 确认两处定位同步更新，旧版仍能打开。

完成标准：用户可以修正真实识别错误，且不必手动分别编辑整体版和分章版。

### M06：Web 书籍转换与目录定位

状态：待实现。

- [ ] 将已验收书籍用例接入现有 Web，上传/转换/下载复用统一来源和产物契约。
- [ ] 展示层级目录、处理报告、历史结果及整体/分章下载，修复嵌套路径寻址等现有边界问题。
- [ ] 迁移本节点用到的 Web 模块至目标目录，保留原启动入口；尚未迁移格式继续使用兼容适配。
- [ ] 提供 `start_web.py`，打印地址、data dir 和停止方法，并说明所需环境准备。

用户验证：启动 → 上传 EPUB → 发起转换 → 在目录中定位章/节 → 下载整包 → 重启后查看历史。所用 Markdown 预览方式与锚点兼容范围应在交付时说明。

完成标准：无需命令行转换即可完成已验收 EPUB 流程，Web 与 CLI 的目录和产物一致。

### M07：其他书籍来源逐个接入

状态：待实现。**以下三个子节点各自完成一次交付和用户验收，不合并为一次大改动。**

- [ ] **M07-DOCX**：按标题样式/编号建立章节树，导出双 MD，保留基础图表；交付 `convert_book.py --input "书.docx"`。用户对照标题、层级、图片与表格验收。
- [ ] **M07-HTML**：按语义标题/section 导入，支持明确的本地资源根路径；交付 HTML 导入参数。用户验证章节与资源，不依赖原 EPUB 导出的特定目录名。
- [ ] **M07-MD**：用 Markdown AST 导入标题、正文和引用；交付 MD 导入参数。用户验证代码中的伪标题不会被切章、中文标题和原有链接有明确处理结果。

每个子节点的完成标准：该来源可以通过相同入口得到双 MD 与统一索引，实际支持范围在报告和能力列表中正确显示。

### M08：文字 PDF、章节与原页定位

状态：待实现。

- [ ] 建立页索引、原生文字与坐标提取，保存空白页和失败页状态。
- [ ] 优先使用有效书签建立章节，结合正文证据；章节可从页中开始并跨页。
- [ ] 生成双 MD 与来源映射，支持查看“这一章来自哪些页”；无可靠章节时明确未分章，不虚构结构。
- [ ] 支持页范围转换、保留基础图片/原页预览；需要 OCR 的页面明确报告。
- [ ] 扩展 `convert_book.py` 的 PDF 参数，提供 `inspect_book.py` 按章节/页查看位置。

用户验证：选带书签的文字 PDF → 转一小段页范围 → 对照章界、空白页、正文顺序 → 从章节查原页 → 打开完整导出。

完成标准：文字书可阅读、源页可追溯，扫描或失败区域不会被静默当成已识别。

### M09：PDF 图片、表格与复杂版面

状态：待实现。

- [ ] 对图片保存原图或带来源的裁剪图；处理旋转/裁剪页的坐标转换。
- [ ] 保留表格单元格、跨度与来源，支持复杂表格展示和跨页片段关联。
- [ ] 优化多栏阅读顺序、页眉页脚过滤与跨页段落，低置信合并保留可检查证据。
- [ ] 扩展现有转换/检查脚本，交付小页范围的具体命令和图表产物位置。

用户验证：挑含图、合并表格、跨页表格或多栏的几页 → 对照原 PDF 与 MD → 检查图表数据、顺序和来源裁剪 → 反馈准确页码/区域。

完成标准：选定复杂案例达到用户认可的可读程度；未恢复结构有明确降级，截图不冒充结构化表格成功。

### M10：现有 OCR 服务与混合 PDF

状态：待实现。开始前确认用户可执行的 OCR 服务及接口能力。

- [ ] 将现有 HTTP OCR 封装为独立 provider，配置脱离 Web。
- [ ] 按页/区域决定原生提取与 OCR；结合真实接口能力实现结果归一化、去重与页状态。
- [ ] 支持服务健康提示、超时、失败报告及重跑失败部分；整文件服务不冒称可按页恢复。
- [ ] 提供 OCR 配置示例和转换命令；若服务只能返回整份 MD，无页映射则保留限制，此节点不能按完整页级能力验收。

用户验证：确认服务可达 → 转扫描页 → 转文字/扫描混合书 → 检查顺序和重复文字 → 临时停止服务观察错误，再恢复执行。

完成标准：实际 OCR 服务能完成约定样书，未识别页、服务故障与恢复范围清晰可见。外部服务本身的部署工作单独说明，不假定仓库已经提供。

### M11：本地小模型 OCR

状态：待实现。设备、内存/显存、语言和模型运行条件待本节点开始时收集。

- [ ] 根据用户设备和真实样书确定一个首期模型/provider，记录模型版本与实际能力。
- [ ] 实现独立模型 worker、加载复用、资源预算、有界并发和页面结果缓存。
- [ ] 完成旋转/裁剪坐标映射、取消/恢复和错误提示；不静默回退到远端。
- [ ] 提供模型准备说明、`start_ocr_worker.py` 和本地 OCR 转换命令，明确首次下载及磁盘要求。

用户验证：启动模型 → 转少量代表页 → 检查文字/表格效果与资源占用 → 连续转第二本 → 更改 MD 展示再次导出 → 中断并恢复任务。

完成标准：用户设备上可持续运行且效果可接受；展示层变化不会重跑 OCR，失败范围与恢复状态可追踪。

### M12：统一导出 HTML 和排版 PDF

状态：待实现。不以 M11 已完成为前提。

- [ ] 从已有 Book IR 生成带层级导航的 HTML，再生成排版 PDF；复用统一资源与链接解析。
- [ ] 明确源页码与新 PDF 页码不同，报告当前支持的图表/样式范围。
- [ ] 将字体、样式和模板纳入正式包资源，整理对应旧模块并保留兼容入口。
- [ ] 扩展 `export_book.py --format html|pdf`，提供实际系统依赖和运行说明。

用户验证：从已导入书籍导出 → 检查中文、目录、图片、表格和分页 → 再导出 MD 确认原有能力仍可用。

完成标准：已保存的书籍可输出可读 HTML/PDF；这是重新排版，保留原版面的可搜索 PDF 另列后续需求。

### M13：批量书库转换与任务恢复

状态：待实现。

- [ ] 批量导入目录，显示每本书的状态、报告与输出位置，单本失败不覆盖其他结果。
- [ ] 基于 SQLite 阶段记录、租约和 token 防重复提交，恢复中断任务；区分缓存复用与新修订。
- [ ] 提供适用的任务并发限制和资源保留策略，长期书籍不按临时文件 TTL 删除。
- [ ] 提供 `convert_library.py`、`resume_job.py` 与状态查询命令。

用户验证：选小批书运行 → 观察各自结果 → 中断进程 → 恢复 → 重复提交 → 确认已完成产物仍在、未完成部分正确继续。

完成标准：可用于实际批量书库，不因一次失败重跑所有书，任务和文件状态一致。

## 6. 可选后续节点

这些节点保持独立交付，按用户需要启动。每个节点继续沿用脚本、人工步骤、反馈修复和明确验收的要求。

| TODO | 可用功能与实现范围 | 脚本交付 | 用户验证与完成标准 |
|---|---|---|---|
| [ ] M14 | AI 分块：保留祖先章节路径、完整语义块、来源和两份 MD 地址 | `export_ai.py` | 用实际下游工具读取分块；任一块能回到原章/源页，大表格拆分关系明确 |
| [ ] M15 | URL 获取快照，经 HTML Reader 导入；修复旧来源地址与存储位置混用 | `import_url.py` | 导入网页，检查标题/图片/来源；重复导出使用同一快照，刷新产生新来源 |
| [ ] M16 | DOCX Writer：标题层级、基础图表与内部引用 | 扩展 `export_book.py` | 在用户使用的 Word/WPS 等工具打开，对照章节与资源，限制可见 |
| [ ] M17 | EPUB Writer：由章节树生成导航与阅读顺序，打包资源 | 扩展 `export_book.py` | 在用户实际阅读器检查目录、翻章和图文；重新导入确认结构可识别 |

保留原版面的可搜索 PDF、主题/人名索引、更复杂检索、额外 OCR 模型和多机部署暂不列入当前必做节点；用户提出后再定义独立可验收功能。

## 7. 目录迁移与架构约束

- 新实现按书籍设计的 `domain/application/structure/readers/export/writers/adapters/interfaces` 组织；只创建当前节点需要的模块。
- SQLite 最小闭环从 M01 随书籍功能接入，后续按历史、修订、任务恢复逐步扩充，不先建完全部未来表。
- 每个输入 Reader 必须保有 MD 输出路径。整体/分章 MD 共用章节树和导出计划，禁止各自识别目录或生成不同身份。
- 每次移动旧模块均包含调用方和资源路径调整；旧入口通过兼容壳逐步接入，不批量重命名后再让用户发现无法启动。
- 安装与启动准备随对应节点提供；需要用户执行包安装检查时，给出独立命令和明确观察点，不变成自动化测试项目。
- 当前 `books/`、`output/` 等用户文件保持原处，运行脚本通过参数选择来源和数据目录。

## 8. 反馈与节点记录模板

用户反馈尽量包含以下信息，缺少部分也可以先描述问题；Codex 应先利用现有报告定位，再提出必要的补充问题。

```text
节点：M01
执行命令：
输入书籍/格式：
job/export ID（终端或报告中可见）：
问题所在章节/页码/输出文件：
预期表现：
实际表现：
终端报错或 report.json 中的相关内容：
结论：通过 / 需要修复
```

每次交付在当前节点下追加记录：

```text
交付日期：
状态：待用户验证
本轮功能/修复：
实际执行命令：
输出位置：
用户反馈摘要：
未解决项：
验收结论与日期：
```

后续开始实现时，默认从 **M01：一本 EPUB 的双版本 Markdown 闭环** 开始。M01 交付后由用户运行和反馈，当前节点修复完成并确认可用后再推进 M02。
